===========================================================================
SEGURANÇA
===========================================================================

[15] Regras do Firestore não estão no repositório
     Não existe firestore.rules nem firebase.json versionado. Sem
     conferir, não dá pra afirmar que users/{uid} e as subcoleções
     (records, achievements, crisisSessions, missions) só são
     lidas/escritas pelo dono. Se o projeto ainda estiver em modo teste,
     qualquer pessoa autenticada lê os dados de todo mundo.
     Fazer: escrever, versionar e publicar as rules
     (match /users/{uid}/{document=**} com request.auth.uid == uid).

[16] debug.keystore commitado
     Está na raiz do repositório. O .gitignore ignora *.jks mas não
     debug.keystore. Tirar do versionamento (git rm --cached).

[17] Sem App Check / reCAPTCHA
     (mesmo item do topo) Sem App Check a API do Firestore/Auth aceita
     chamada de qualquer cliente com a config pública. Vale ligar App
     Check junto com o reCAPTCHA do login.

===========================================================================
QUALIDADE / INFRA (o que falta no projeto)
===========================================================================

[21] Sem ErrorBoundary: qualquer exceção em render derruba o app inteiro
     na tela branca, sem log e sem "tenta de novo".

[22] app.json com version "1.0.0", buildNumber "1" e versionCode 1
     fixos — vai travar a segunda submissão nas lojas.

[24] CalendarioMensal usa `whiteSpace: 'nowrap'` no StyleSheet — é CSS de
     web, inválido no nativo.

[25] obterNivel() com XP negativo devolve "Lendário" (utils/xp.js).
     O findIndex não acha faixa pra xp < 0, devolve -1, e o fallback
     `indiceEncontrado === -1 ? NIVEIS.length - 1 : ...` joga pro último
     nível em vez do primeiro. Hoje calcularXp() só soma parcelas não
     negativas, então não dá pra chegar lá pela UI — é bug latente, e
     vira real no dia em que aparecer qualquer XP descontado.
     Fazer: clampar o índice em 0 quando o xp for menor que NIVEIS[0].minimo.
     Já existe teste documentando o comportamento atual em
     __tests__/utils/xp.test.js ("XP negativo cai no ultimo nivel")
     ao corrigir, inverter a expectativa desse teste pra 'Iniciante'.

===========================================================================
FEATURES / IDEIAS (backlog)
===========================================================================

- Importar backup JSON (a exportação já existe, falta o par).
- Editar/apagar sessão de crise (hoje só dá pra criar e ver).
- Gráfico de economia acumulada ao longo do tempo.
- Comparativo "esta semana x semana passada" na Home (a conta já existe
  em mediaDiariaNasDatas, só falta a UI).
- Lembrete agendado no horário de risco do usuário (periodoDasCrises já
  calcula o período em que a vontade mais bate).
- Permitir escolher a data final da meta no calendário, em vez de só
  30/60/90 dias.
- Widget / notificação com ação rápida "registrei hoje".
